machine:
  ruby:
    version:
      2.4.0
test:
  override:
    - bundle install: # note ':' here.
        timeout: 180
    - jeykll build
  post:
    - git submodule sync

deployment:
  prod:
    branch: master
    commands:
      - "aws s3 sync ./_site/ s3://ds.limited/ --delete"
